import react from 'react';

const recruitOccupationX = (hardWorking, experienced, educated, kind) => {
  const isMatch = hardworking && experienced && educated && kind;
  if (isMatch) {
    return true;
  } else {
    return false;
  }
}

const attractAwesomeness = (Fillipi) => {
  const isAwesome = CandidateFirstName && CandidateLastName && awesome;
  if (isAwesome) {
    return true;
  } else {
    return false;
  }
}